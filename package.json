{
  "name": "foo",
  "version": "1.0.0",
  "description": "replicate rollup bug",
  "main": "src/scripts/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-js": "rollup -c -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.36.0",
    "rollup-watch": "^2.5.0"
  }
}
